package MxEntity;

/**
 * The scope of block is exactly the father scope plus
 * the statement above the block statement;
 */
public class BlockEntity extends Entity {
    public BlockEntity(String id) {
        super(id);
    }
}
